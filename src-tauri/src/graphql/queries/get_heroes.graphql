query GetHeroes($modType: ModType!) {
    heroesNew(modType: $modType) {
        heroes {
            entities {
                id,
                race,
                name
            }
        }
    }
}